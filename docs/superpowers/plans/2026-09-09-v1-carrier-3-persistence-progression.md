# Abyss Eater V1 Carrier 3 Persistence and Progression Implementation Contract

Carrier 3 starts only after Carriers 1-2 are merged and production-qualified. Before coding, replace this contract with a full task-by-task plan against the then-current `main` tree.

Required scope: persistent guest identity, signed session credential, D1 schema/migrations, profile API, XP/level progression, pearls, cosmetic catalog/ownership, transactional pearl spending, idempotent rewards and durable seasonal/all-time leaderboard. Hot movement/snapshot processing must not write D1 per input. No real-money payment path is permitted.