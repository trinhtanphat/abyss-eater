# Abyss Eater V1 Carrier 2 MVP Polish Implementation Contract

Carrier 2 starts only after Carrier 1 is merged and production-qualified. Before any Carrier 2 code change, refresh this file into a full task-by-task implementation plan against the then-current `main` tree.

Required scope from the approved V1 spec: responsive loading/game/death/reconnect flows, settings, quality presets, audio controls, keyboard-accessible menus, mobile safe-area controls, reduced-effects mode, unsupported-browser/WebGL state, PWA/service-worker shell caching, and production asset hardening without adding paid services.

Carrier 2 must preserve the Carrier 1 protocol/reconnect contract and server authority.