# Carrier 1 TDD Gate

Production changes begin only after a failing test commit is pushed on `feat/v1-carrier1-multiplayer-hardening` and pull-request CI demonstrates the intended RED state. Subsequent implementation must make that same behavior GREEN before merge.