# V1 Carrier Sequencing

`design/v1-full-abc` is the design/planning base. Implementation branches are cut from this base for Carrier 1; after Carrier 1 merges, subsequent implementation branches are cut from the latest `main` and include the approved V1 spec/planning docs through the Carrier 1 merge. This prevents later work from forking stale production code.