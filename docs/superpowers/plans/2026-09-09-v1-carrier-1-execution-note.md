# Carrier 1 Execution Note

Execution mode: inline via GitHub connector on an isolated feature branch. TDD RED is demonstrated by pull-request CI before production helpers are added; GREEN is demonstrated on the same PR head after implementation. The user has already authorized continue-all integration/deployment subject to CI and verification gates.